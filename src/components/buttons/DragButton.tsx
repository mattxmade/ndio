import { MinusIcon } from "lucide-react";
import IconButton from "./IconButton";

const DragButton = () => {
  return (
    <IconButton>
      <MinusIcon />
    </IconButton>
  );
};

export default DragButton;
