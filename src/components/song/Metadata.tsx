import { PlayIcon, HeartIcon, Clock1Icon } from "lucide-react";

type MetadataProps = {
  skipDate: boolean;
} & React.ComponentPropsWithRef<"section">;

const Metadata = (props: MetadataProps) => {
  return <section />;
};

export default Metadata;
